# API REST - GESTIÓN EQUIPOS

Ejecutar en docker

1. crear imagen

<code> docker build -t "rest-equipos" . </code>

2. correr en docker

<code> docker run --name gestion-equipos -p 8080:8080 rest-equipos:lastest </code>
